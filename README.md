# PapaControl

자동으로 구르고 활공하는 Phaser 3 기반 모바일 대응 2D 플랫포머 프로토타입입니다.

```bash
pnpm install
pnpm dev
```

검증은 `pnpm lint`, `pnpm test`, `pnpm build` 순서로 실행합니다. `main` 브랜치에 push하면 GitHub Actions가 GitHub Pages에 자동 배포합니다.
